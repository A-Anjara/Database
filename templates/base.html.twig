<!DOCTYPE html>
<html>

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		{% block stylesheets %}{% endblock %}

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
	</head>

	<body class=" bg-teal-50 font-inter	">
		{% block header %}
			<header class="bg-white p-1 sm:p-2 flex items-center justify-between sticky top-0">
				<h1 class=" text-teal-600 font-semibold text-xl">BSpay<sub>db</sub>
				</h1>
				<nav class="flex items-center justify-end gap-6">
					<a href="/" class=" text-black hover:text-teal-700 max-sm:text-sm">Accueil</a>
					<a href="{{ path('export') }}">Exporter</a>
				</nav>
				<div class="text-xl">
					<a href="{{ path('logout') }}" class="text-red-500 hover:text-red-700 max-sm:text-sm">Sortir</a>
				</div>
			</header>
		{% endblock %}
		<main class="p-1 sm:p-2 md:p-3"> {% block body %}{% endblock %}
			</main>
		</body>

	</html>
